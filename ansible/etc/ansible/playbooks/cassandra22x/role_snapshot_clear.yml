---
# ansible-playbook /etc/ansible/playbooks/cassandra22x/role_snapshot_clear.yml --extra-vars "target=ansible_inventory_hosts snapshot_clear_yml=snapshot_clear_yml"
- hosts: "{{ target | default('FAIL') }}"
  gather_facts: false
  any_errors_fatal: true
  roles:
    - cassandra22x_snapshot_clear