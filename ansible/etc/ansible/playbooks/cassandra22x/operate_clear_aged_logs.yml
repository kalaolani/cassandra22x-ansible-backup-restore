---
# ansible-playbook /etc/ansible/playbooks/cassandra22x/operate_clear_aged_logs.yml --extra-vars "target=ansible_inventory_hosts"
- hosts: "{{ target | default('FAIL') }}"
  gather_facts: false
  any_errors_fatal: true
  roles:
    - cassandra22x_clear_aged_logs